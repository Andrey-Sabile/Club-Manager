services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: andreysabile
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ClubManager
    ports:
      - "5432:5432"
  web:
    build:
      context: ./src
      dockerfile: Dockerfile
    depends_on:
      - db